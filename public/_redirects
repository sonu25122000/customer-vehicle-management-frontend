# Netlify-style proxy config, kept for reference / in case this app is ever
# hosted on Netlify. The live deployment is on Vercel, where vercel.json's
# "rewrites" (not this file) is what actually proxies /api/* in production.
/api/*  https://customer-vehicle-management-backend.vercel.app/api/:splat  200
